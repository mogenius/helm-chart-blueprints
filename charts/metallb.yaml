apiVersion: v1
kind: HelmBlueprint
metadata:
  id: metallb
  displayName: MetalLB
  description: Bare-metal load-balancer for Kubernetes — assigns external IPs on clusters without cloud load-balancers
  category: networking
  icon: metallb
  tags: [loadbalancer, networking, bare-metal]
spec:
  repository:
    name: metallb
    url: https://metallb.github.io/metallb
  chart:
    name: metallb/metallb
    version: "0.15.3"
  install:
    release: metallb
    namespace: metallb-system
  values: |
    # Configure address pools after installation via IPAddressPool and L2Advertisement CRDs
    speaker:
      frr:
        enabled: false

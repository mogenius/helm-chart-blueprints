apiVersion: v1
kind: HelmBlueprint
metadata:
  id: flux-operator
  displayName: Flux Operator
  description: Kubernetes operator for managing Flux CD instances declaratively using a FluxInstance CRD
  category: gitops
  icon: flux
  tags: [gitops, flux, cicd, kubernetes, operator]
spec:
  repository:
    name: flux-operator
    url: oci://ghcr.io/controlplaneio-fluxcd/charts
  chart:
    name: flux-operator/flux-operator
    version: "0.15.0"
  install:
    release: flux-operator
    namespace: flux-system
  values: |
    # After installation, create a FluxInstance CR to configure Flux components
